# Copy to .env and fill with your real values. Do NOT commit .env
DISCORD_TOKEN=REPLACE_ME

# Channel IDs
MTG_SPOILERS_CHANNEL_ID=REPLACE_ME
UB_SPOILERS_CHANNEL_ID=REPLACE_ME
BOT_TESTING_CHANNEL_ID=REPLACE_ME

# Schedule & behavior
POST_HOUR=9
POST_MINUTE=0
TZ=America/Chicago
BULK_DIR=bulk_cache
WINDOW_DAYS=1
STATE_PATH=state.json
POST_DELAY_MS=700

# Choose: prints | cards (prints = every printing; cards = roll up reprints)
SCRYFALL_UNIQUE=prints
